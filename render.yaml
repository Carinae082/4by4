services:
  - type: web
    name: pygame-stage1
    runtime: static
    buildCommand: python -m pip install -r requirements.txt && python -m pygbag --ume_block 0 --build game
    staticPublishPath: game/build/web
